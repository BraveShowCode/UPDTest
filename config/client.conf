# UDP multicast discovery client configuration
multicast_group = 239.255.0.1
multicast_port = 30001
bind_address = 0.0.0.0
interface = 0.0.0.0

# Use 0 for an ephemeral source port; server replies to this UDP port by unicast.
listen_port = 0
timeout_ms = 3000
